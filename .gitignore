# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
chainlightning
samplebackend/timestampserver/timestampserver

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# config files
*.toml
